# FreqHub Frontend Environment Variables
# Copy this file to .env and adjust the values as needed

# Base Path Configuration
# For subdirectory deployments (e.g., /freqhub/), set this to the base path
# Default: / (root)
VITE_BASE_PATH=/

# API Proxy Target (Optional)
# In development, Vite proxy uses this to forward /api requests to the backend
# In production, leave empty to use relative URLs (Nginx will proxy)
# Default: http://localhost:3001 (for development)
VITE_API_PROXY_TARGET=http://localhost:3001

# Config Service URL (Optional)
# URL of the FreqHub Config Service for config management
# Default: http://localhost:3005 (for development)
VITE_CONFIG_SERVICE_URL=http://localhost:3005

# Config Service API Key (Optional)
# API key for authenticating with the Config Service
# In production, set this to your secure API key
VITE_CONFIG_SERVICE_API_KEY=
